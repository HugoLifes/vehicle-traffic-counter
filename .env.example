# Copia este archivo a .env y pon tu key real ahí.
# .env está en .gitignore — nunca se sube al repositorio.
#
# Se genera gratis (sin tarjeta) en https://build.nvidia.com
NVIDIA_API_KEY=nvapi-tu-key-aqui
